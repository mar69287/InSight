import sendRequest from './send-request'
const BASE_URL = '/api/'

export async function createEmployee(companyData) {
    // return sendRequest(`${BASE_URL}/create`, 'POST', companyData)
    console.log('create employee api')
}